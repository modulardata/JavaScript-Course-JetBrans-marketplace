// for testing purposes, these two lines need to be commented
// const fs = require('fs');
// 'use strict';

// getFirstUserData delivers data of the first user
// for testing purposes, fs must be passed as parameter to the function

function getFirstUserData(fs, userFilePath) {
/* Implement promise chaining as documented. In the last then statement you must return a Promise that is resolved */
}

// getFirstUserData delivers data of the first user
// for testing purposes, fs must be passed as parameter to the function
async function getFirstUserDataAsync(fs, userFilePath) {
    /* Implement the same behavior using async/await */
};
